# Engine settings
# backend options: "openai", "anthropic", "ollama", "deepseek", "xai", "phind", "google", "groq", "api"

# provider = "openai"
# max_tokens = 1024

# provider = "google"
# max_tokens = 1024

provider = "ollama"
model = "qwen2.5-coder:0.5b"
# max_tokens = 4096